export { default } from 'next-auth/middleware'

export const config = {
    matcher: [
        // '/tourist/tours/join/:path*',
        // '/tourist/tours/member/:path*',
        // '/agency/:path*'
    ]
}